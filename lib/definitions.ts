type RandomDogImgResponse = {
	fileSizeBytes: string;
	url: string;
};

type RandomDogFactResponse = {
	facts: string[];
	success: boolean;
};

type AnimeQuoteResponse = {
	quote: string;
	anime: string;
	character: string;
};
